[project]
name = "swin-implementation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "einops>=0.8.2",
    "numpy>=2.4.3",
    "timm>=1.0.25",
    "torch>=2.10.0",
]
